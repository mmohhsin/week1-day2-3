<% @posts.each do |post|%>
    <h1><%= post.name %></h1>
    <p><%= post.body %></p>
    <br>
   
  <%= link_to "Delete Post", admin_post_path(post), data: { turbo_method: :delete, turbo_confirm: "U Sure?"} %>
<% end %>
