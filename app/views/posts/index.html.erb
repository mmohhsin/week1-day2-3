<% @posts.each do |post|%>
    <h1><%= post.name %></h1>
    <p><%= post.body %></p>
    <br>
    <%= link_to "Read Post", post %><br>
  <%= link_to "Delete Post", post_path(post), data: { turbo_method: :delete, turbo_confirm: "U Sure?"} %>
<% end %>
